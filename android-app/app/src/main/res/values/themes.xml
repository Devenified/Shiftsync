<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Main application theme - bright, modern, Material 3 -->
    <style name="Base.Theme.Verson1" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/brand_secondary</item>
        <item name="colorSecondaryVariant">@color/brand_secondary_dark</item>
        <item name="colorOnSecondary">@color/white</item>

        <item name="android:colorBackground">@color/bg_main</item>
        <item name="colorOnBackground">@color/text_primary</item>
        <item name="colorSurface">@color/bg_card</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorError">@color/status_error</item>

        <item name="android:statusBarColor">@color/brand_primary_dark</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:navigationBarColor">@color/bg_card</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>

        <item name="materialAlertDialogTheme">@style/ThemeOverlay.ShiftSync.Dialog</item>
        <item name="bottomSheetDialogTheme">@style/ThemeOverlay.ShiftSync.BottomSheet</item>

        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>

    <style name="Theme.Verson1" parent="Base.Theme.Verson1" />

    <!-- Full-bleed theme for screens that want their own status bar color
         (set programmatically or via attribute) -->
    <style name="Theme.Verson1.Transparent" parent="Theme.Verson1">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/bg_card</item>
    </style>

    <style name="ThemeOverlay.ShiftSync.Dialog" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="materialAlertDialogTitleTextStyle">@style/TextAppearance.ShiftSync.DialogTitle</item>
    </style>

    <style name="ThemeOverlay.ShiftSync.BottomSheet" parent="ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Widget.ShiftSync.BottomSheet</item>
    </style>

    <style name="Widget.ShiftSync.BottomSheet" parent="Widget.MaterialComponents.BottomSheet.Modal">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.ShiftSync.BottomSheet</item>
        <item name="backgroundTint">@color/bg_card</item>
    </style>

    <style name="ShapeAppearance.ShiftSync.BottomSheet" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">24dp</item>
        <item name="cornerSizeTopRight">24dp</item>
    </style>

    <style name="TextAppearance.ShiftSync.DialogTitle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
